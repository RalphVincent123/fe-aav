import ResetPassword from "@/components/ResetPassword";
import ResetPasswordPassword from "@/components/ResetPassword";

export default function ResetPasswordPage() {
  return (
    <main
      id="main"
      style={{
        width: "100%",
        height: "100%",
        position: "sticky",
        background: "#E5E4E3",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <ResetPassword />
    </main>
  );
}
